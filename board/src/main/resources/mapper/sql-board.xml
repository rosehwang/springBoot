<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="board.board.mapper.BoardMapper">
	<select id="selectBoardList" resultType="board.board.dto.BoardDto">
	<![CDATA[
	SELECT
		board_idx,
		title,
		hit_cnt,
		created_datetime
	FROM
		t_board
	WHERE
		deleted_yn = 'N'
	ORDER BY board_idx DESC		
	]]>
	</select>
	
	<insert id="insertBoard" parameterType="board.board.dto.BoardDto" useGeneratedKeys="true" keyColumn="board_idx" keyProperty="boardIdx">
		<![CDATA[
				INSERT INTO T_BOARD 
				( 
					board_idx,
					title,
					contents,
					hit_cnt,
					created_datetime,
					creator_id,
					updated_datetime,
					updator_id,
					deleted_yn
				)values(
					BOARD_SEQ.NEXTVAL,
					#{title},
					#{contents},
					0,
					sysdate,
					'admin',
				    sysdate,
				    'admin',
				    'N'
				)
			]]>
	</insert>
	
	<select id="selectBoardDetail" parameterType="int" resultType="board.board.dto.BoardDto">
		<![CDATA[
			SELECT
				board_idx,
				title,
				contents,
				hit_cnt,
				TO_CHAR(created_datetime,'YYYY.MM.DD HH24:MI:SS') AS created_datetime,
				creator_id
			FROM
				t_board
			WHERE
				board_idx = #{boardIdx}
				AND deleted_yn = 'N'
		]]>
	</select>
	
	<update id="updateHitCount" parameterType="int">
		<![CDATA[
			UPDATE 
				t_board 
			SET 
				hit_cnt = hit_cnt + 1 
			WHERE 
				board_idx = #{boardIdx}
		]]>
	</update>
	
	<update id="updateBoard" parameterType="board.board.dto.BoardDto">
		<![CDATA[
			UPDATE t_board SET 
				title = #{title},
				contents = #{contents},
				updated_datetime = sysdate,
				updator_id = 'admin'
			WHERE 
				board_idx = #{boardIdx}
		]]>
	</update>
	
	<update id="deleteBoard" parameterType="int">
		<![CDATA[
			UPDATE t_board SET 
				deleted_yn = 'Y',
				updated_datetime = sysdate,
				updator_id = 'admin'
			WHERE 
				board_idx = #{boardIdx}
		]]>
	</update>
	
	
	<insert id="insertBoardFileList" parameterType="board.board.dto.BoardFileDto">
		<![CDATA[
			INSERT INTO t_file
			(	idx,
				board_idx,
				original_file_name,
				stored_file_path,
				file_size,
				creator_id,
				created_datetime,
				updated_datetime,
				updator_id,
				deleted_yn
			)
			VALUES
		]]>
		<foreach collection="list" item="item" separator=",">
			(	BOARD_FILE_SEQ.NEXTVAL,
				#{item.boardIdx},
				#{item.originalFileName},
				#{item.storedFilePath},
				#{item.fileSize},
				'admin',
				sysdate,
				sysdate,
				'admin',
				'N'
			)
		</foreach>
	</insert>
	
	<select id="selectBoardFileList" parameterType="int" resultType="board.board.dto.BoardFileDto">
		<![CDATA[
			SELECT
				idx,
				board_idx,
				original_file_name,
				TO_CHAR(ROUND(file_size / 1024), '999,999,999') AS file_size
			FROM
				t_file
			WHERE
				board_idx = #{boardIdx}
				AND deleted_yn = 'N'
		]]>
	</select>
	
	<select id="selectBoardFileInformation" parameterType="map" resultType="board.board.dto.BoardFileDto">
		<![CDATA[
			SELECT
				original_file_name,
				stored_file_path,
				file_size
			FROM
				t_file
			WHERE
				idx = #{idx}
				AND board_idx = #{boardIdx}
				AND deleted_yn = 'N'
		]]>
	</select>
</mapper>